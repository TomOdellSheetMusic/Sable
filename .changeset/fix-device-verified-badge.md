---
default: patch
---

Fix devices showing as verified in Sable while other clients still see them as unverified, and let an unverified device start emoji verification instead of only offering the recovery key
